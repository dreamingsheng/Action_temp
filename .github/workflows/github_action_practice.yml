name: learn-github-actions

on: 
  #手动触发
  workflow_dispatch:

jobs:
  first-run-test:
    #指定运算环境，最新的Ubuntu下
    runs-on: ubuntu-latest
    steps:
      #执行 bash 命令
      - run: echo "🐧 hello,this is my first step!"
